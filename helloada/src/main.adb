-- A Tribute to Ada Lovelace
-- To run open a terminal in the directory where the file is saved and type
-- gnatmake hello.adb
with Ada.Text_IO; use Ada.Text_IO;
procedure Main is
begin
   --  Insert code here.
   Put_Line("Hello Ada Lovelace!");
   null;
end Main;
